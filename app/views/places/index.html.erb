<div class="container">
<h1>PLACES LIST</h1>
<ul>
  <% @places.each do |place| %>
    <li><%= link_to place.name, place_path(place[:id]) %></li>
    <p><%= place.country %></p>
    <p><%= place.description %></p>
    <% end %>
</div>
</ul>
